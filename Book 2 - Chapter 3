var name;

name = "Mieke";

console.log(name.length);
console.log(name.indexOf("v"));

var question = prompt("Is this your name?" + name);

alert("Welcome back " + question + " Your name matched our variable: " + name);

document.write("Welcome to the document " + name);
